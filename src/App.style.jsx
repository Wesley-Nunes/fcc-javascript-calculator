import styled from 'styled-components';

const StyledMain = styled.main`
  background-color: hsl(180, 78%, 2%);

  max-width: 26.4rem;

  padding-top: 10rem;

  border-radius: 5% 5% 0 0;
`;

export default StyledMain;
